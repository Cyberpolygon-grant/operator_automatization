# Автоматизация оператора ДБО для Windows

Автоматизация работы оператора ДБО: скачивание файлов из почты и автоматическое открытие Excel файлов для запуска VBA макросов.

## 🚀 Быстрый старт

### Вариант 1: Простой запуск (для тестирования)

1. Настройте `dbo_operator_config.py`:
```python
EMAIL_ADDRESS = "operator1@financepro.ru"
EMAIL_PASSWORD = "1q2w#E$R"
IMAP_SERVER = "localhost"
```

2. Запустите:
```batch
start_dbo_automation.bat
```

### Вариант 2: Защищенный запуск (рекомендуется для продакшена)

Запускает скрипт в фоновом режиме с автоматическим перезапуском при сбое:

```batch
start_windows_service.bat
```

**Особенности:**
- Запускается скрыто (без окна консоли)
- Автоматически перезапускается при сбое
- Защищен от простого завершения
- Логи сохраняются в `dbo_windows_service.log`

**Остановка:**
```batch
stop_windows_service.bat
```

### Вариант 3: Установка как службы Windows (автозапуск)

Устанавливает задачу в планировщике задач для автозапуска при входе в систему:

```batch
REM Запустите от имени администратора
install_windows_service.bat
```

## 📁 Структура файлов

```
dbo_operator_automation/
├── dbo_operator_automation.py    # Основной скрипт автоматизации
├── dbo_operator_config.py         # Конфигурация
├── start_dbo_automation.py        # Упрощенный запуск
├── dbo_windows_service.py         # Windows сервис с защитой
├── start_dbo_automation.bat      # Простой запуск
├── start_windows_service.bat      # Защищенный запуск
├── stop_windows_service.bat       # Остановка сервиса
├── install_windows_service.bat    # Установка как службы
└── README.md                       # Документация
```

## 🔧 Настройка

Откройте `dbo_operator_config.py` и измените настройки:

```python
EMAIL_ADDRESS = "operator1@financepro.ru"  # Email оператора
EMAIL_PASSWORD = "1q2w#E$R"                # Пароль
IMAP_SERVER = "localhost"                  # IMAP сервер
IMAP_PORT = 993                             # IMAP порт
CHECK_INTERVAL = 30                         # Интервал проверки (сек)
AUTO_OPEN_EXCEL = True                      # Автооткрытие Excel
MODE = 'continuous'                         # Режим работы
```

## 📊 Логирование

Логи сохраняются в папке `dbo_operator_automation/`:
- **`dbo_operator_automation.log`** - основной лог работы автоматизации
  - Все действия: подключение, скачивание, открытие файлов
  - Подробная информация о каждом письме и файле
- **`dbo_windows_service.log`** - лог Windows сервиса (если используете `start_windows_service.bat`)
  - Информация о запуске/перезапуске процесса
  - Ошибки сервиса

### Как открыть логи:

1. **Через проводник Windows:**
   - Откройте папку `dbo_operator_automation`
   - Найдите файл `dbo_operator_automation.log`
   - Откройте его в Блокноте или любом текстовом редакторе

2. **Через командную строку:**
   ```batch
   cd путь\к\dbo_operator_automation
   notepad dbo_operator_automation.log
   ```

3. **Прямой путь:**
   ```
   C:\Users\[ВашеИмя]\Downloads\mail\dbo_operator_automation\dbo_operator_automation.log
   ```

Подробнее см. файл `ГДЕ_СМОТРЕТЬ_ЛОГИ.txt`

## 🛡️ Защита от завершения

Windows сервис (`dbo_windows_service.py`) обеспечивает:
- Автоматический перезапуск при сбое (до 10 попыток)
- Скрытый запуск (без окна консоли)
- Мониторинг процесса каждые 10 секунд
- Защиту от случайного завершения

## ⚠️ Важные замечания

1. **Права доступа**: Для установки как службы требуются права администратора
2. **Python**: Убедитесь, что Python установлен и доступен в PATH
3. **Зависимости**: Установите `psutil` для работы Windows сервиса:
   ```batch
   pip install psutil
   ```
4. **Excel**: Убедитесь, что Excel установлен и макросы включены
5. **Безопасность**: Пароль хранится в открытом виде в конфиге

## 🔍 Проверка работы

### Проверка запущенных процессов

```batch
tasklist | findstr python
```

### Просмотр логов

Откройте файлы:
- `dbo_operator_automation.log`
- `dbo_windows_service.log`

### Проверка через планировщик задач

```batch
schtasks /Query /TN "DBO Operator Automation"
```

## 🐛 Устранение неполадок

### Процесс не запускается

1. Проверьте логи в `dbo_windows_service.log`
2. Убедитесь, что Python установлен
3. Проверьте настройки в `dbo_operator_config.py`

### Процесс завершается сразу

1. Проверьте логи в `dbo_operator_automation.log`
2. Убедитесь, что IMAP сервер доступен
3. Проверьте правильность учетных данных

### Excel файлы не открываются

1. Убедитесь, что Excel установлен
2. Проверьте, что макросы включены в Excel
3. Проверьте логи на наличие ошибок

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в файлах `.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки IMAP сервера
4. Убедитесь, что Excel установлен и доступен

