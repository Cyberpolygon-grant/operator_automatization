# Автоматизация оператора ДБО

Система автоматизации для оператора ДБО (Digital Banking Operations), которая отслеживает файлы из Docker-контейнера или удаленного сервера через SSH, скачивает `.xlsm` файлы (Excel с макросами) и автоматически открывает их для выполнения VBA макросов.

## 🚀 Возможности

- ✅ **Мониторинг файлов** - автоматическое отслеживание новых файлов из контейнера или удаленного сервера
- ✅ **SSH поддержка** - подключение к удаленному серверу через SSH/SFTP
- ✅ **Локальный режим** - работа с Docker-контейнером через локальную файловую систему
- ✅ **Автоматическое открытие** - автоматическое открытие `.xlsm` файлов для запуска макросов
- ✅ **Автозакрытие** - автоматическое закрытие Excel файлов через заданное время
- ✅ **Автозапуск** - возможность запуска при входе в систему Windows
- ✅ **Скрытый режим** - работа в фоновом режиме без видимого окна консоли
- ✅ **Автоперезапуск** - автоматический перезапуск при завершении работы
- ✅ **Очистка файлов** - автоматическое удаление старых скачанных файлов
- ✅ **Логирование** - подробное логирование всех операций

## 📋 Требования

- **Python 3.6+**
- **Windows** (для автозапуска и работы с Excel)
- **Опционально:**
  - `paramiko` - для SSH подключения (если используется удаленный сервер)
  - `pywin32` - для работы с COM объектами Excel (опционально)

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd operator_automatization
```

### 2. Установка зависимостей

#### Для локального режима (Docker):
```bash
pip install -r requirements.txt
```

#### Для SSH режима:
```bash
pip install paramiko
```

#### Для работы с Excel (опционально):
```bash
pip install pywin32
```

### 3. Настройка конфигурации

Откройте файл `1/dbo_automation.py` и настройте параметры в секции **КОНФИГУРАЦИЯ**:

```python
# SSH режим
USE_SSH = True
SSH_HOST = "10.18.2.6"
SSH_USER = "iux"
SSH_PASSWORD = "InfoTecs1830"
SSH_PORT = 22
REMOTE_ATTACHMENTS_DIR = "/home/iux/mail/sent_attachments"

# Локальный режим
USE_SSH = False
CONTAINER_ATTACHMENTS_DIR = "./sent_attachments"

# Общие настройки
DOWNLOAD_DIR = "C:/Users/YourUser/Downloads"
CHECK_INTERVAL = 5  # секунды
AUTO_OPEN_EXCEL = True
EXCEL_CLOSE_DELAY = 7  # секунды
FILE_LIFETIME_MINUTES = 10
```

## 🎯 Использование

### Ручной запуск

```bash
cd 1
python dbo_automation.py
```

### Установка автозапуска (Windows)

⚠️ **ВАЖНО:** Все файлы должны находиться в:
```
C:\Program Files\operator_automatization-main\1\
```

#### Быстрая установка:

1. **Скопируйте необходимые файлы** в директорию:
   ```
   C:\Program Files\operator_automatization-main\1\
   ```
   
   Необходимые файлы:
   - `dbo_automation.py` - основной Python скрипт
   - `start_automation.bat` - обертка с автоперезапуском
   - `start_automation_hidden.vbs` - VBS скрипт для скрытого запуска

2. **Запустите установку автозапуска:**
   ```bash
   cd "C:\Program Files\operator_automatization-main\1"
   install_autostart.bat
   ```

3. **Готово!** Скрипт будет запускаться **СКРЫТО** при входе в систему и автоматически перезапускаться при завершении. Система **НЕ будет спрашивать о доверии** (используется ярлык).

#### Особенности автозапуска:

- ✅ Python скрипт запускается при входе в систему
- ✅ Работает **СКРЫТО** (консоль не видна пользователю)
- ✅ Автоматически перезапускается при завершении (ошибка, закрытие и т.д.)
- ✅ Работает в фоновом режиме
- ✅ Все файлы в Program Files
- ✅ Нет запроса о доверии (используется ярлык вместо прямого VBS)
- ✅ Обрабатываются только файлы, созданные **ПОСЛЕ** запуска ПК
- ✅ Старые файлы автоматически пропускаются
- ✅ Файлы удаляются через 10 минут после скачивания
- ✅ При завершении Python скрипта - автоматически перезапускается через 3 секунды

#### Проверка работы:

Откройте **Диспетчер задач** (`Ctrl+Shift+Esc`) и найдите процессы:
- `cmd.exe` (start_automation.bat) - обертка с автоперезапуском
- `python.exe` (dbo_automation.py) - основной скрипт

Если процессы есть - скрипт работает!

### Удаление автозапуска

#### Способ 1: Через скрипт

```bash
cd "C:\Program Files\operator_automatization-main\1"
uninstall_autostart.bat
```

#### Способ 2: Вручную

1. **Остановите процессы:**
   - Откройте Диспетчер задач (`Ctrl+Shift+Esc`)
   - Найдите процесс `cmd.exe` (start_automation.bat)
   - Завершите процесс
   - Или найдите `python.exe` с командной строкой содержащей `dbo_automation.py` и завершите его

2. **Удалите ярлык из автозагрузки:**
   - Нажмите `Win+R`
   - Введите `shell:startup`
   - Удалите файл `dbo_automation_startup.lnk`

## 📁 Структура проекта

```
operator_automatization/
├── README.md                          # Документация
├── check_downloads_xlsm.bat           # Скрипт проверки Downloads
├── check_downloads_xlsm_hidden.vbs    # VBS для скрытого запуска
└── 1/
    ├── dbo_automation.py              # Основной Python скрипт
    ├── start_automation.bat           # Обертка с автоперезапуском
    ├── start_automation_hidden.vbs    # VBS для скрытого запуска
    ├── install_autostart.bat          # Установка автозапуска
    └── uninstall_autostart.bat        # Удаление автозапуска
```

## ⚙️ Конфигурация

### SSH режим

Используется для подключения к удаленному серверу:

```python
USE_SSH = True
SSH_HOST = "your-server-ip"
SSH_USER = "username"
SSH_PASSWORD = "password"
SSH_PORT = 22
REMOTE_ATTACHMENTS_DIR = "/path/to/attachments"
```

### Локальный режим

Используется для работы с Docker-контейнером:

```python
USE_SSH = False
CONTAINER_ATTACHMENTS_DIR = "./sent_attachments"
```

### Параметры работы

- `CHECK_INTERVAL` - интервал проверки новых файлов (секунды)
- `AUTO_OPEN_EXCEL` - автоматически открывать `.xlsm` файлы
- `EXCEL_CLOSE_DELAY` - время до автоматического закрытия Excel (секунды)
- `FILE_LIFETIME_MINUTES` - время жизни скачанных файлов (минуты)
- `PROCESS_ALL_FILES` - обрабатывать все файлы заново (игнорировать список обработанных)

## 🔍 Как это работает

1. **Мониторинг** - скрипт периодически проверяет директорию на наличие новых файлов `*_metadata.json`
2. **Обработка метаданных** - для каждого найденного JSON файла загружаются метаданные письма
3. **Скачивание** - вложения из метаданных копируются в папку Downloads
4. **Открытие** - файлы `.xlsm` автоматически открываются в Excel
5. **Закрытие** - через заданное время Excel файлы автоматически закрываются
6. **Очистка** - старые файлы удаляются через заданное время

